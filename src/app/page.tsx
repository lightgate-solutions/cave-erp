import HomePage from "./(marketing)/page";

export default function RootPage() {
  // Always show the marketing landing page
  // Users can access the dashboard via /dashboard route
  return <HomePage />;
}
